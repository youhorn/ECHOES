# ECHOES — Unity 완전 초보자 세팅 가이드

> Unity를 처음 켜보는 사람 기준으로, **화면 보는 법부터** 하나하나 설명합니다.
> 코드(21개 스크립트)는 이미 다 작성돼 있어요. 이 문서는 그 코드를
> Unity 안에서 **조립해서 실제로 플레이되게 만드는** 과정입니다.
>
> 👉 목표: 먼저 **Stage 1 하나를 끝까지 플레이되게** 만들어 성공 경험을 얻고,
>    나머지 스테이지는 같은 패턴을 반복합니다. (자세한 건 `SETUP_GUIDE.md` 참고)
>
> ⏱️ 예상 소요: 천천히 따라 하면 2~3시간. 중간중간 ▶로 실행해 확인하며 갑니다.

---

# 0장. 시작하기 전에 — 마음가짐

- **한 번에 다 하려 하지 마세요.** 이 문서는 "작은 성공"을 여러 번 쌓는 구조입니다.
  각 장 끝의 `✅ 실행 체크`에서 ▶를 눌러 동작을 꼭 확인하세요.
- **모르는 단어가 나와도 멈추지 마세요.** 1~2장에서 용어를 다 설명합니다.
- **자주 저장하세요.** 단축키 `Cmd+S`(맥). 씬이 바뀔 때마다 저장 습관.
- 화면이 내 설명과 조금 달라도 당황 금지 — Unity 버전/레이아웃 차이일 뿐입니다.

---

# 1장. Unity 화면 익히기

Unity를 켜고 ECHOES 프로젝트를 열면 화면이 여러 칸(창)으로 나뉘어 있습니다.
각 창이 뭔지부터 알면 길을 잃지 않아요.

```
┌─────────────┬──────────────────────────┬───────────────┐
│             │                          │               │
│  Hierarchy  │      Scene / Game        │   Inspector   │
│  (계층)      │      (장면 / 게임화면)     │   (속성)       │
│             │                          │               │
├─────────────┴──────────────────────────┤               │
│              Project (프로젝트 파일)        │               │
│              Console (콘솔/로그)           │               │
└────────────────────────────────────────┴───────────────┘
```

| 창 | 한 줄 설명 | 무엇에 쓰나 |
|---|---|---|
| **Hierarchy** (계층) | 현재 장면(씬)에 들어 있는 **물건 목록** | 캐릭터·바닥·UI 같은 걸 여기서 만들고 고름 |
| **Scene** (장면) | 게임 세계를 **위에서 편집하는 뷰** | 물건을 끌어다 배치·이동 |
| **Game** (게임) | 실제 플레이어가 볼 **게임 화면 미리보기** | ▶를 누르면 여기서 게임이 돌아감 |
| **Inspector** (속성) | 고른 물건의 **세부 설정판** | 위치, 색, 스크립트 값 등을 바꿈 |
| **Project** (프로젝트) | 디스크에 저장된 **모든 파일** (스크립트·이미지·씬) | 에셋을 꺼내 씀 |
| **Console** (콘솔) | **에러/메시지** 출력창 | 빨간 글씨 = 문제. 자주 확인! |

> Console 창이 안 보이면 상단 메뉴 **Window ▸ General ▸ Console** 로 띄우세요.
> 작업 내내 Console를 한쪽에 켜두는 걸 추천합니다.

---

# 2장. 꼭 알아야 할 4가지 개념

딱 4개만 이해하면 됩니다.

### ① GameObject (게임오브젝트)
게임 속 **물건 하나**. 캐릭터, 바닥, 버튼, 카메라… 전부 GameObject입니다.
처음엔 **빈 상자**예요. 여기에 아래 ②를 붙여서 기능을 줍니다.

### ② Component (컴포넌트)
GameObject에 붙이는 **부품/기능**. 예:
- `Sprite Renderer` = 그림을 보이게 하는 부품
- `Rigidbody 2D` = 중력·물리를 받게 하는 부품
- `Box Collider 2D` = 부딪힘 판정 상자
- `PlayerController`(우리가 만든 스크립트) = "이동·점프시키는" 부품

> **즉, "GameObject = 상자, Component = 그 상자에 끼우는 기능 카드"** 라고 생각하세요.

### ③ Prefab (프리팹)
잘 조립한 GameObject를 **도장처럼 찍어 재사용**하는 틀.
플레이어를 한 번 잘 만들어 프리팹으로 저장하면, 다른 씬에서 끌어다 바로 씁니다.

### ④ Scene (씬)
하나의 **무대/맵**. 우리 게임은 `Title`, `Stage1`…`Stage5` 씬을 만듭니다.
씬을 바꾸면 무대가 통째로 바뀝니다.

---

# 3장. 기본 조작 4가지 (이것만 익히면 됨)

앞으로 계속 반복할 동작들입니다. 손에 익혀두세요.

### 조작 A. 빈 GameObject 만들기
Hierarchy 빈 공간에서 **마우스 우클릭 ▸ Create Empty**
→ 빈 상자 하나 생성. 이름은 더블클릭하거나 F2로 변경.

### 조작 B. 컴포넌트(기능) 붙이기
1. Hierarchy에서 대상 GameObject를 클릭(선택)
2. 오른쪽 **Inspector** 맨 아래 **`Add Component`** 버튼 클릭
3. 검색창에 부품 이름 입력 (예: `Rigidbody 2D`, `PlayerController`) → 클릭

### 조작 C. 슬롯에 끌어다 넣기 (가장 중요!)
스크립트 설정 중 `Ground Layer`, `Player`, `Slash Effect Prefab` 같은
**빈 칸(슬롯)** 이 보일 때가 있어요. 거기엔 보통:
- **Hierarchy의 GameObject**를 마우스로 잡아 그 칸에 **끌어다 놓거나**,
- **Project의 파일**(프리팹·스프라이트)을 끌어다 놓습니다.

> 예: HUDController의 `Hp Fill` 칸에 → Hierarchy의 HP바 Image를 드래그.

### 조작 D. 저장 / 실행
- 저장: `Cmd+S`
- 실행: 화면 **상단 가운데 ▶(Play)** 버튼. 다시 누르면 정지.
- ⚠️ **재생 중에 바꾼 값은 정지하면 사라집니다.** 설정은 정지 상태에서 하세요.

---

# 4장. 프로젝트 기본 설정 (10분)

게임 만들기 전, 딱 한 번 해두는 환경 설정입니다.

### 4-1. 레이어(Layer) 3개 추가
레이어 = 오브젝트를 그룹으로 묶는 **이름표**. "이건 바닥, 이건 적" 구분에 씀.

1. 아무 GameObject나 하나 선택 → Inspector 맨 위 오른쪽 **`Layer ▾`** 드롭다운 클릭
2. 맨 아래 **`Add Layer...`** 클릭
3. 비어 있는 칸(User Layer 6,7,8…)에 차례로 입력:
   - `Ground`
   - `Player`
   - `Enemy`
4. (입력만 하면 됩니다. 할당은 나중에 각 물건에서.)

### 4-2. 태그(Tag) 확인
태그 = 또 다른 이름표. 코드가 "플레이어"를 찾을 때 씁니다.
- 같은 드롭다운 위쪽 **`Tag ▾`** 에 `Player`가 있는지 확인 (기본 존재).
  없으면 `Add Tag...`로 `Player` 추가.

### 4-3. 게임 해상도 480×270
1. **Game** 창 상단 왼쪽 해상도 드롭다운 클릭 → 맨 아래 **`+`**
2. Label: `ECHOES`, Type: `Fixed Resolution`, Width `480`, Height `270` → OK → 선택

### 4-4. 카메라 픽셀 설정 (선택이지만 권장)
1. Hierarchy에서 **Main Camera** 선택
2. Inspector ▸ Camera ▸ Projection을 **Orthographic**, Size `8.4375`
3. (선택) `Add Component` ▸ `Pixel Perfect Camera`
   - Assets Pixels Per Unit `16`, Reference Resolution `480 × 270`

`✅ 실행 체크`: 아직 볼 건 없지만, Console에 빨간 에러가 없으면 OK.

---

# 5장. 첫 성공 — "바닥 위에서 움직이는 캐릭터" (가장 중요한 단계)

여기서 처음으로 "내가 만든 게임"이 움직입니다. 천천히 따라오세요.
지금은 그림 대신 **네모(플레이스홀더)** 로 만듭니다. (그림은 맨 마지막에 교체)

### 5-1. 바닥 만들기
1. Hierarchy 우클릭 ▸ **2D Object ▸ Sprites ▸ Square** → 네모 생성. 이름 `Ground`로 변경
2. Inspector ▸ Transform ▸ Scale을 `X=20, Y=1` 로 (길고 납작한 바닥)
3. 위치 Position `Y = -3` 정도로 내려서 화면 아래에 깔기
4. **Layer를 `Ground`로 변경** (Inspector 우상단 Layer 드롭다운)
5. `Add Component` ▸ **`Box Collider 2D`** 추가 (부딪힘 판정. Is Trigger는 **끄기**)
6. 색 구분: Sprite Renderer ▸ Color를 회색으로

### 5-2. 플레이어 만들기
1. Hierarchy 우클릭 ▸ **2D Object ▸ Sprites ▸ Square** → 이름 `Player`
2. **Tag를 `Player`로 변경** (Inspector 우상단 Tag 드롭다운) — ⚠️ 매우 중요
3. Sprite Renderer ▸ Color를 보라색으로, 위치는 바닥 위쯤(`Y=0`)
4. `Add Component`로 아래 4개 부품 추가:
   - **`Rigidbody 2D`** → 추가 후 Gravity Scale `3`, 그리고
     `Constraints ▸ Freeze Rotation Z` **체크** (캐릭터가 쓰러지지 않게)
   - **`Box Collider 2D`**
   - **`PlayerController`** (우리 스크립트 — 검색창에 그대로 입력)
   - **`PlayerHealth`** / **`PlayerAttack`** 도 같이 추가
5. **PlayerController** 컴포넌트에서 **`Ground Layer`** 칸 클릭 ▸ `Ground` 체크
   - (이걸 안 하면 "바닥을 인식 못 해 점프가 안 됨" — 1번 단골 실수)

### 5-3. 실행!
`✅ 실행 체크`: ▶ 누르고
- **A / D** (또는 ← →) → 좌우 이동
- **Space** → 점프, 바닥에 착지
- 보라 네모를 클릭하면 발밑에 **초록 사각형(접지 검사 박스)** 이 보임

> 안 되면? Console 확인 → 빨간 에러 메시지를 그대로 저에게 보여주세요.
> 흔한 원인: Ground Layer 미지정 / 바닥 Layer가 Ground가 아님 / Rigidbody 안 붙임.

🎉 여기까지 되면 절반은 온 겁니다! 정지(▶ 다시) 후 `Cmd+S` 저장.

### 5-4. 프리팹으로 저장 (재사용용)
1. Project 창에 `Assets` 안에서 우클릭 ▸ Create ▸ Folder → `Prefabs` 폴더 생성
2. Hierarchy의 `Player`를 마우스로 잡아 → Project의 `Prefabs` 폴더에 **끌어다 놓기**
   → 파란색 프리팹이 생김. 이제 다른 씬에서 끌어다 쓸 수 있어요.

---

# 6장. 씬 6개 만들고 등록하기

무대(씬)를 만들 차례입니다.

### 6-1. 씬 만들기
1. Project ▸ `Assets/Scenes` 폴더 (없으면 만들기)
2. 지금 작업하던 씬을 `Cmd+S` → 이름 `Stage1`로 저장
   (또는 File ▸ New Scene ▸ Basic 2D ▸ 저장)
3. 같은 식으로 빈 씬을 만들어 `Title`, `Stage2`, `Stage3`, `Stage4`, `Stage5` 저장
   - 빠른 방법: Project에서 `Stage1` 씬 파일 복사(Cmd+D)해 이름만 바꿔도 됨

### 6-2. Build Settings에 등록 (필수!)
씬 전환이 되려면 등록이 필요합니다.
1. **File ▸ Build Settings**
2. 각 씬을 하나씩 열어둔 상태로 **`Add Open Scenes`** 클릭, 또는
   Project에서 씬 파일들을 목록으로 **끌어다 놓기**
3. `Title`, `Stage1`~`Stage5` 6개가 목록에 보이면 OK. 창 닫기.

> 등록 안 하면 게임 중 "Scene couldn't be loaded" 에러가 납니다.

---

# 7장. GameManager — 게임의 두뇌

기억·열쇠·씬 전환을 **모든 씬에서 공유**하는 핵심입니다. `Title` 씬에 둡니다.

1. `Title` 씬 열기 (Project에서 더블클릭)
2. Hierarchy 우클릭 ▸ Create Empty → 이름 `GameManager`
3. `Add Component` ▸ **`GameManager`** 추가
4. Inspector에서:
   - **Title Scene**: `Title` 이라고 입력
   - **Stage Scenes**: 오른쪽 작은 숫자칸(Size)에 `5` 입력 → 칸 5개 생기면
     위에서부터 `Stage1`, `Stage2`, `Stage3`, `Stage4`, `Stage5` 입력
5. `Cmd+S` 저장

> 한 번만 만들면 게임 내내 살아남습니다(다른 씬으로 가도 안 사라짐).
> 기억 문장 5개와 "거짓=④"는 코드에 들어 있어 따로 입력할 게 없습니다.

---

# 8장. 타이틀 화면 (Title 씬)

### 8-1. 캔버스(UI 판) 만들기
1. `Title` 씬에서 Hierarchy 우클릭 ▸ **UI ▸ Canvas**
   → Canvas와 EventSystem이 자동 생성됨 (EventSystem은 버튼 클릭 감지용, 건드리지 마세요)
2. Canvas 선택 ▸ Canvas Scaler 컴포넌트 ▸ UI Scale Mode를
   **`Scale With Screen Size`**, Reference Resolution `480 × 270`

### 8-2. 버튼 2개 만들기
1. Hierarchy의 **Canvas** 우클릭 ▸ UI ▸ **Button - TextMeshPro**
   - 처음이면 "Import TMP Essentials" 팝업 → **Import** 클릭 (한 번만)
2. 버튼 이름 `StartButton`. 자식 Text를 더블클릭해 글자를 `게임 시작`으로
3. 같은 식으로 `ExitButton` 만들고 글자 `종료`
4. Scene에서 두 버튼을 화면 가운데로 적당히 배치

### 8-3. TitleMenu 연결
1. Hierarchy 우클릭 ▸ Create Empty → `TitleMenu`
2. `Add Component` ▸ **`TitleMenu`** 추가. First Stage Scene = `Stage1` 확인
3. **StartButton** 선택 ▸ Inspector의 Button ▸ **On Click ()** 칸:
   - `+` 클릭 → 왼쪽 슬롯에 Hierarchy의 **TitleMenu** 드래그
   - 오른쪽 함수 드롭다운 ▸ **TitleMenu ▸ StartGame**
4. **ExitButton** 도 같은 방식으로 함수 = **TitleMenu ▸ QuitGame**

`✅ 실행 체크`: ▶ → `게임 시작` 클릭 → Stage1으로 넘어가면 성공!
(Stage1이 5장에서 만든 그 씬입니다.)

---

# 9장. Stage 1 꾸미기 — HUD · NPC · 문

이제 Stage1을 "진짜 스테이지"로 만듭니다. 이 패턴을 익히면 2~5도 똑같이 합니다.

## 9-A. HUD (체력·열쇠·기억 표시)

1. `Stage1` 씬에서 Hierarchy 우클릭 ▸ **UI ▸ Canvas** (Scaler는 8-1처럼 설정)
2. Canvas 자식으로 만들기:
   - **HP 바**: Canvas 우클릭 ▸ UI ▸ **Image** → 이름 `HPFill`
     - Inspector ▸ Image ▸ **Image Type = `Filled`**, Fill Method `Horizontal`
     - 색을 빨강으로, 가로로 길게
   - **HP 글자**: UI ▸ Text - TextMeshPro → 이름 `HPText` (예: `100/100`)
   - **열쇠 글자**: TMP → `KeyText` (예: `x 0`)
   - **기억 글자**: TMP → `MemoryText` (예: `기억 0 / 5`)
3. Hierarchy 우클릭 ▸ Create Empty → `HUD` ▸ `Add Component` ▸ **`HUDController`**
4. HUDController 칸에 **드래그로 연결**:
   - `Hp Fill` ← Hierarchy의 `HPFill`
   - `Hp Text` ← `HPText`
   - `Key Text` ← `KeyText`
   - `Memory Text` ← `MemoryText`

`✅ 실행 체크`: ▶ → 화면에 HP/열쇠/기억 표시가 뜨고, 플레이어가 다치면 HP 숫자가 줄면 OK.
(다치게 하려면: 빈 네모 + Box Collider 2D(Is Trigger ✔) + `ContactDamage` 만들고
 Target Layers를 `Player`로 → 닿으면 HP 감소·깜빡임 확인)

## 9-B. NPC 대화 (기억① + 열쇠 지급)

### 먼저 대화창(UI) 만들기
1. Canvas 우클릭 ▸ UI ▸ Image → `DialogueBox` (대화창 배경, 화면 아래쪽)
2. `DialogueBox` 자식으로:
   - TMP → `SpeakerText` (화자 이름 자리)
   - TMP → `BodyText` (대사 본문 자리)
3. `DialogueBox`를 일단 **비활성화**: 선택 후 Inspector **맨 위 이름 옆 체크박스 끄기**
   (평소엔 숨겨두고, 대화할 때만 코드가 켭니다)
4. Hierarchy 우클릭 ▸ Create Empty → `DialogueSystem` ▸ Add Component ▸ **`DialogueSystem`**
   - `Dialogue Panel` ← `DialogueBox`
   - `Speaker Text` ← `SpeakerText`
   - `Body Text` ← `BodyText`
   - (선택지/Continue Indicator는 Stage1에선 비워도 됨)

### NPC 본체 만들기
1. 2D Object ▸ Sprites ▸ Square → `NPC` (색 구분)
2. `Add Component` ▸ **`Box Collider 2D`** ▸ **Is Trigger 체크** (다가가면 감지되는 범위)
   - 범위를 넓히려면 Collider의 Size를 키우세요
3. `Add Component` ▸ **`NPCInteraction`**:
   - **Speaker Name**: `기억지기`
   - **Lines**: Size를 `3` 등으로 → 각 줄에 대사 입력
     (예: `에코야... 드디어 왔구나.` / `이 공간은 네가 스스로 만든 세계란다.` / `기억의 파편을 모아 진실을 되찾아라.`)
   - **Choices**: Size `0` (Stage1은 단순 대화)
   - **Reward Memory Id**: `1`
   - **Reward Keys**: `1`

`✅ 실행 체크`: ▶ → NPC에 가까이 가서 **E** → 대사가 한 글자씩 출력 → E로 넘기기 →
끝나면 기억이 `1/5`, 열쇠 `x 1`로 바뀌면 성공!

## 9-C. 문 (열쇠로 열고 Stage2로)

1. 2D Object ▸ Sprites ▸ Square → `Door` (스테이지 끝쪽에 배치)
2. `Add Component` ▸ **Box Collider 2D** ▸ **Is Trigger 체크**
3. `Add Component` ▸ **`DoorController`**:
   - **Go To Next Stage** 체크 (다음 스테이지로 자동 이동)
   - (Locked/Open Sprite는 지금은 비워도 동작)

`✅ 실행 체크`: ▶ → NPC에게 열쇠를 받은 뒤 문 앞에서 **E** → 잠시 후 Stage2로 전환되면 성공!
(열쇠 없이 E를 누르면 "열쇠가 없습니다" 로그가 Console에 뜸)

🎉 **여기까지 오면 Stage 1이 완전히 플레이됩니다!** 이게 게임의 기본 뼈대예요.

---

# 10장. 나머지 스테이지 (같은 패턴 반복)

Stage 2~5는 위에서 배운 것의 응용입니다. 각 씬에 **HUD(9-A)** 를 똑같이 깔고,
아래 핵심만 추가하면 됩니다. (세부 슬롯은 `SETUP_GUIDE.md`에 정리돼 있어요)

### Stage 2 — 선택지 대화
- 9-B처럼 NPC를 만들되 **Choices** Size를 2~4로 채움
  - 각 선택지: `Text`(문구) / `Is Correct`(정답에만 체크) / `Response Text`(반응 대사)
  - `Reward Memory Id` = `2`
- 대화창에 선택지 버튼 UI(`ChoicePanel` + Button들)를 추가하고 DialogueSystem에 연결
- 게임에서 **↑↓로 선택, Z로 확인**. 정답이어야 보상 지급.

### Stage 3 — 순서 퍼즐
- 빈 오브젝트 + **`PuzzleManager`**: `Correct Order`에 정답 순서(예: 2,4,1,3),
  `Reward Memory Id`=3, `Reward Object`=정답 시 켤 문
- 조각마다 네모 + Box Collider 2D(Trigger) + **`PuzzlePiece`** (각자 `Piece Id`, `Manager` 연결)
- 게임에서 조각 앞 **E**, 정답 순서로 누르면 클리어.

### Stage 4 — 보스
- 투사체 프리팹: 빨간 네모 + Collider 2D(Trigger) + **`BossProjectile`** → Prefabs에 저장
- 보스: 큰 네모 + Rigidbody 2D + Collider 2D + **`BossController`**, **Layer = `Enemy`**
  - `Projectile Prefab` ← 위 투사체 프리팹
  - `Fire Point` ← 보스 앞에 만든 빈 오브젝트
  - `Left X` / `Right X` = 보스가 오갈 좌우 범위
  - `Reward Memory Ids` = `4, 5`
- 게임에서 **Z/J로 타격** → HP 50%에 페이즈2(빨라짐·투사체 부채꼴·순간이동) → 처치 시 Stage5로.

### Stage 5 — 엔딩 선택
- 5줄 문장 UI(TMP) + `TrueEndingPanel` / `BadEndingPanel`(둘 다 비활성)
- 빈 오브젝트 + **`EndingController`**: `Entry Labels`(5칸) 연결, 결과 패널 연결
- 게임에서 **↑↓ 선택, Z 확인** → **④(거짓)** 제거 시 진실 엔딩!
- ⚠️ 5개 문장은 앞 스테이지를 다 거쳐 모았다는 전제. 테스트만 하려면
  Stage1→5를 순서대로 플레이하거나, 임시로 GameManager에서 미리 수집하게 두세요.

---

# 11장. 기억 인벤토리 (Tab 키, 선택 기능)

각 스테이지에서 Tab으로 모은 기억을 보는 창입니다.
1. Canvas 자식 `MemoryPanel`(비활성) + 5줄(TMP) + 카운터 TMP
2. 빈 오브젝트 + **`MemoryUI`**: `Panel`, `Entry Texts`(5), `Counter Text` 연결
3. 게임 중 **Tab**으로 열고 닫기.

---

# 12장. 그림(아트) 입히기 — 맨 마지막에

게임 로직이 다 돌아가면 그림을 교체합니다. **스크립트는 안 건드려요. 그림만 갈아끼웁니다.**

### 12-1. 에셋 가져오기
- `echoes_assets_v2` 폴더의 PNG들을 Unity **Project 창의 `Assets/Art`** 폴더로
  드래그해 넣기 (탐색기/파인더에서 끌어다 놓기)

### 12-2. 임포트 설정 (픽셀 아트 공통)
가져온 PNG를 클릭 → Inspector에서:
- Texture Type: **Sprite (2D and UI)**
- Pixels Per Unit: **16**
- Filter Mode: **Point (no filter)** ← 픽셀이 또렷해짐
- Compression: **None**
- (스프라이트 시트면) Sprite Mode: **Multiple** → **Sprite Editor**에서 Slice
- 다 바꾸면 아래 **Apply** 클릭

> ⚠️ 주의: 우리 v2 시트는 "IDLE (4 frames)" 같은 **글자가 박힌 레퍼런스 이미지**예요.
> 그대로 Slice하면 글자까지 잘립니다. 캐릭터 프레임 부분만 깨끗하게 잘라
> 새 PNG로 만들어 쓰는 게 좋습니다. (처음엔 네모 그대로 둬도 게임은 잘 돌아갑니다)

### 12-3. 교체
- 각 GameObject의 **Sprite Renderer ▸ Sprite** 칸에, UI는 **Image ▸ Source Image** 칸에
  원하는 스프라이트를 드래그해 끼우면 끝.

### 12-4. 한글 폰트 (□ 두부 방지)
TMP 글자가 □로 보이면 한글 폰트가 없어서예요.
1. **Window ▸ TextMeshPro ▸ Font Asset Creator**
2. Source Font에 한글 폰트(예: Noto Sans CJK KR) 지정 → **Generate Font Atlas** → Save
3. 각 TMP 텍스트의 Font Asset을 방금 만든 걸로 변경

### 12-5. 애니메이션 (선택, 심화)
- 캐릭터가 움직이는 애니는 **Animator Controller**가 필요합니다.
- 우리 코드가 보내는 신호(파라미터): `Speed`, `IsGrounded`, `VerticalVelocity`,
  `Jump`, `Attack`, `Hurt`, `Death` (보스: `Idle/Dash/Shoot/Teleport/Death`, `Phase2`)
- 이건 분량이 커서, 게임이 다 돌아간 뒤 따로 도와드릴게요.

---

# 13장. 문제가 생기면 (자주 막히는 곳)

| 증상 | 원인 / 해결 |
|---|---|
| 점프가 안 됨 / 공중에서 무한 점프 | PlayerController의 **Ground Layer** 미지정, 또는 바닥의 Layer가 `Ground`가 아님 |
| NPC·문·퍼즐이 **E에 반응 안 함** | 플레이어 **Tag=`Player`** 누락 / 상대 콜라이더 **Is Trigger** 안 켬 |
| 공격해도 보스가 안 맞음 | 보스 **Layer=`Enemy`** ≠ PlayerAttack의 **Target Layers** |
| 씬 전환에서 에러 | 씬을 **Build Settings에 등록** 안 함 / GameManager의 Stage Scenes 이름 오타 |
| HUD가 안 변함 | HUDController 슬롯 미연결 / 그 씬에 GameManager가 안 옴(타이틀부터 시작해야 함) |
| 글자가 □(두부) | TMP 한글 폰트 미설정 (12-4 참고) |
| Console 빨간 에러 | 메시지를 **그대로 복사해서 저에게** 보여주세요. 같이 해결해요. |

> **디버깅 1순위 = Console 창.** 빨간 줄을 클릭하면 어느 스크립트·몇 번째 줄인지 나옵니다.

---

# 14장. 추천 진행 순서 요약

1. 4장: 레이어·태그·해상도 (10분)
2. 5장: 바닥+캐릭터 움직이기 → **▶로 확인** (가장 중요한 첫 성공)
3. 6장: 씬 6개 + Build Settings
4. 7장: GameManager (Title 씬)
5. 8장: 타이틀 버튼 → **▶로 Stage1 진입 확인**
6. 9장: Stage1에 HUD·NPC·문 → **▶로 끝까지 플레이 확인** ← 여기까지면 뼈대 완성
7. 10~11장: Stage2~5, 기억창 (같은 패턴 반복)
8. 12장: 그림·폰트·애니 입히기 (맨 마지막)

각 단계 끝에서 ▶로 확인하며 가면, 어디서 틀어졌는지 바로 알 수 있어요.
막히면 그 단계 화면과 Console 에러를 알려주세요. 함께 풀어갑시다. 🙂
```
